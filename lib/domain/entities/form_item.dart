class FormItem {
  final String title;
  final String url; // The destination link (e.g., Google Form URL)
  final String iconPath; // Path to the document icon asset

  const FormItem({
    required this.title,
    required this.url,
    required this.iconPath,
  });
}
